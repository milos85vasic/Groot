buildscript {
    repositories {
        maven {
            url uri('../repo')
        }
    }
    dependencies {
        classpath group: 'net.milosvasic.gradle', name: 'Groot', version: '1.0-SNAPSHOT'
    }
}

apply plugin: "groot-kotlin"
apply plugin: "groot-kotlin-application"

application.setup("net.milosvasic.try.groot")

